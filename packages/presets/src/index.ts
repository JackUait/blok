export { fetchStorage } from './fetch-endpoint';
export { supabaseStorage } from './supabase';
export { presignedStorage } from './presigned';
export { cloudinaryStorage } from './cloudinary';

// Remaining storage presets are re-exported here as each one ships (task 6: IndexedDB).
