# https://blokeditor.com/robots.txt
#
# Everything on this property is public documentation and is open to every
# crawler, including AI training and AI-answer crawlers.
#
# Nothing under /assets/ is disallowed on purpose. Every route is prerendered to
# a real HTML file, so a crawler that runs no JavaScript already sees the prose —
# but the bundles still carry interactive content and the client-side router, and
# a crawler that does render is entitled to the same page a reader gets.

User-agent: *
Allow: /
# Cloudflare's Content-Signal directive (CC0): the machine-readable form of the
# sentence above. `search` = search results, `ai-input` = live AI answer
# context, `ai-train` = model training. Strict validators warn about the
# unrecognised directive; RFC 9309 tells parsers to ignore lines they don't know.
Content-Signal: search=yes, ai-input=yes, ai-train=yes

# --- AI answer engines / retrieval crawlers (explicit opt-in) ---
#
# One group, many tokens: RFC 9309 says a crawler obeys only the most specific
# group matching its token, so a named agent never reads the `*` group above.
# Listing them here without repeating Content-Signal would have told each of
# them nothing.

User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: GPTBot
User-agent: Claude-SearchBot
User-agent: Claude-User
User-agent: ClaudeBot
User-agent: PerplexityBot
User-agent: Google-Extended
User-agent: Applebot-Extended
Allow: /
Content-Signal: search=yes, ai-input=yes, ai-train=yes

Sitemap: https://blokeditor.com/sitemap.xml
