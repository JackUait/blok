# Wave 2 — core inversion (from design agent, 2026-09-24)
Invariant target: flat order == DFS over roots (array order), each parent's children in contentIds order, unlisted children appended in flat order (tolerance).
Steps: 0 validateTreeOrder (report-only, pin failing paths it.fails) | 1 primitives: utils/block-tree.ts (subtreeEnd, flatIndexForPlacement, dfsOrder, placementImpliedByFlat) + Blocks.mount + BlockHierarchy.placeBlock | 2a setBlockParent data path via placeBlock (delete moveInto/OutOfParentRun, childSlotForFlatOrder call) | 2b setBlockParent DOM path via Blocks.mount | 2c insert placement option; route insertInsideParent, insertAt/moveTo, split, transferParentLink | 3 index insert/move become shims | 4 saver + gate read contentIds | 5a DocumentStore placement API + replay | 5b local doc writers pass placement | 5c tools explicit parents (table/db/columns) | 6 delete dead code.
Parallel now: 0, 1, 5a-docstore half. Serial after refs merge: 2a→2b, 2c, 3, 4; 5a-yjs-sync half after 3; 5b after 3+5a; 5c after 2c; 6 last.
Key facts: same-parent moves leave contentIds stale today (setBlockParent splices only when unlisted, hierarchy.ts:496; saver rebuilds from flat hides it; saver.test.ts:664,791,816,838 pin tolerance). parentImpliedByIndex returns undefined inside table/db (block-insertion.ts:728). DOM anchor: before first later tree-order block whose holder.parentElement is home slot, else append (hierarchy.ts:686-694). resolveYjsPlacement blockManager.ts:1280-1303.

## Step 1 notes (notion2/s1)
- Subtree-end copies to consolidate in step 6 onto `subtreeEnd` in src/components/utils/tree-order.ts:59 (exclusive end, cycle-safe, dangling parent ends the chain):
  - block-insertion.ts:750 `subtreeEnd(parent)` (exclusive end)
  - api/block-placement.ts:74 `subtreeEnd(tree, block)` (exclusive end)
  - yjs-sync.ts:1277 `afterRunOf` (exclusive end; a block missing from memory falls back to docIndex, which is caller logic)
  - utils/blocks-tree.ts:411 `subtreeEndIndex` (returns the LAST index, not exclusive; `isDescendantOf` has no cycle guard)
- Cost: `Blocks.mount` scans the array after the block, so `placeBlock` mounting a k-block subtree is O(n·k). Fine for now; an index or slot-local anchor would fix it if large subtrees get slow.
- `validateTreeOrder` now compares against `dfsOrder` (one walk). Cycle rule: blocks cut off by a parent cycle go last, in flat order.
- `BlockHierarchy` takes the blocks store as an optional 4th constructor argument; step 2a must pass it at blockManager.ts (`new BlockHierarchy(...)`), or `placeBlock` throws.

## Step 2c notes (notion2/s2c)
- `BlockManager.insert({ placement: { parentId, afterId } })` (block-insertion.ts `insertAtPlacement`): no index/parent inference; `flatIndexForPlacement` → store insert (appendToWorkingArea) → `placeBlock` (dom) → collapsed-parent hide → block-added `{index: final, parentId}` → Yjs `addBlockAt` + `writeRenderedChildPlacements` (still needed: rendered() fires before the parent reaches the doc). `index`/`replace` with a placement throw; `forceTopLevel`/`appendToWorkingArea`/`inferParent`/`eventParentId` are ignored.
- What placeBlock leaves to callers now lives in `blockManager/new-block-placement.ts`: `isSelfPlacedParent` (table/db or under one → keep the index path) and `hideUnderCollapsedParent`. 2b/5c: use these, and delete hierarchy.ts's copy of the hide rule in step 6.
- Routed: insertInsideParent (clamped index → afterId = last child before it), insertAt root (nested insertAt still goes through insertInsideParent with the index; exact round trip), split + splitBlockWithData (tail = `{parentId: current.parentId, afterId: current.id}`, i.e. after the subtree; splitBlockWithData only when insertIndex === currentIndex + 1), transferParentLinkToNewBlock (afterId = nearest earlier listed id that is still a child; old id removed).
- NOT routed: moveTo (no BlockManager move-by-placement yet; step 3). `ResolvedPlacement` carries both `afterId` and `index`; drop `index` when moveTo switches. `test/unit/helpers/fake-placement.ts` reads `.index`.
- Everything under a table/database (and paste's non-replace path, toolbox/index inserts, insertMany) still uses the index path.

## Step 5c notes (notion2/s5c, columns + database)
- Landed: column tool seed/click-append use `insertAt(undefined, undefined, {parentId, position:'start'|'end'})` (pure delegation to insertInsideParent; characterization snapshot unchanged).
- NOT landed, patches ready: `s5c-column-drop.patch` (wrapInNewColumnList, wrapBlocksInColumns, addColumnToList; deletes indexAfterSubtree), `s5c-seed-columns.patch` (ColumnList.seedColumns), `s5c-database.patch` (both row adds; `position:'end'` — base lands the row LAST, 'start' would change order). Their mock unit tests (column-drop.test, column-list.test, database.test) still assert the old calls.
- Measured with test/unit/tools/column-database-placement.characterization.integration.test.ts: flat, contentIds, mount slots, holder order, saved JSON and undo steps are identical under all three patches. Blocking core gaps (src/components, not 5c's):
  1. `insertInsideParent` always `setCurrentBlockRaw(newBlock)` (block-insertion.ts ~1076), though `InsertAtOptions.focus` says default false (types/api/blocks.d.ts ~61). getCurrentBlockIndex -1 → new block after a drop / row add.
  2. `insertInsideParent` runs in `withAtomicOperation({extendThroughRAF:true})`, so `isSyncingFromYjs` stays true until the next frame. A `setBlockParent` right after it skips BlockMoved AND the tool MOVED hook (blockManager.ts ~1275 guard). Side-drop lost `block-moved` for target and sources; add-column lost it for the sources.
  3. Yjs doc keeps transient creation flags (`noSeed` on list/columns, `noFocus` on seeded columns) that base strips as a side effect of later re-syncs (hypothesis: those re-syncs are what gap 2 suppresses; unverified). Saved JSON unaffected.
- Intended delta once 1-3 are fixed: `block-added` carries the final parent; base's add-at-inferred-parent + `block-moved` pair (and the spurious `block-changed` on the wrongly inferred parent) disappear.
- Moves stay `setBlockParent`: `moveTo` refuses any move into/out of a column (api/block-placement.ts:200) and out of an ownsChildren parent (column_list). Covers column-drop's source/target moves, unwrapColumnListIfCollapsed, rogue eviction.
  4. (hypothesis) `Blocks.insert` calls `YjsManager.beginApiCall()` (api/blocks.ts ~457), `insertAt` (~504) does not. Column-drop runs inside `transact` (which calls it), so only the database patch loses the API gesture; undo steps matched in the characterization (no typing run before the click), real-world effect unverified.
- The characterization snapshot pins base events/current block. It WILL go red when core changes events or the current block; re-baseline (`-u`) only if the diff stays inside `events`/`current`/`doc.data`. Any hunk in flat/contentIds/mountedIn/holderOrder/saved/undoSteps is a regression.

## Step 3 notes (notion2/s3)
- `BlockMutation.move()` (not in a caller's move group), keyboard move up/down (`placeRun`) and `blocks.moveTo` (→ new `BlockManager.moveTo(block, {parentId, afterId})`) place the subtree with `placeBlock`. The re-mount/re-assert loops are gone; what they did besides mounting (re-indent, collapsed-parent hiding, parent data sync) is now explicit: `hierarchy.syncVisibilityWithParent`, `hierarchy.announceChildPlaced`, `hierarchy.reindentSubtree` (now public), `placeBlock({reindent:false})`.
- Hook/event payload VALUES stay byte-identical by REPLAYING the old index moves on a copy of the array (`replayIndexMove`, `replaySubtreeMove`). The live array is never swapped: events fire on the placed (final) array. moved() gets the neighbours the index move left the block between under a symbol key (`INDEX_MOVE_NEIGHBOURS`, src/components/utils/index-move-neighbours.ts, `Symbol.for`), which the list's depth validator reads instead of `getBlockByIndex(toIndex ± 1)` — without it a list parent sees its own structural child next and promotes itself (pinned in blocks-placement 'keeps a list parent at its depth…'). Remove when list nesting stops reading flat neighbours (phase 2).
- A caller's move group (drag) still does the old flat `Blocks.move` (`moveFlat`): DragController reads `parentId` after the move to decide reparent vs reorder, and re-asserts itself. Step 5/6: DragController must pass placements; then `Blocks.move`/`resortNestedBlocks`/`isNested` go (also used by yjs-sync.ts:2184).
- Index insert with an implied parent (api `blocks.insert` inferParent path) → `insertAtPlacement` unless the parent is/under a table/database. Tool demotion extracted to `toolForIndexInsert` so root inserts keep the neighbour-container childTools demotion.
- Index move refuses (silent no-op): a slot in another table cell, entering a table/database as a new parent. Leaving a cell for a slot outside the table stays allowed (worked on base, and now lands in a valid tree even into a container). Keyboard move stops at the cell edge.
- Deleted: `sortListedChildrenByFlatOrder` and the `getIsApplyingFromYjs` hierarchy param/threading. `BlockYjsSync.isRunningOperationBody` (yjs-sync.ts:265) has no consumer now — delete in step 6 (not in s3 ownership).
- moveTo still computes `placement.index` for the "header next to a table cell" refusal (api/blocks.ts) — drop with the :520 pin.

## Step 5a yjs-sync notes (notion2/s5y)
- Replay places by doc placement: `BlockYjsSync.docPlacementInMemory(id, docParent, order)` = doc parent + nearest earlier doc sibling memory holds under it (walk back over the doc DFS order, stop at the parent; a sibling must agree on parentId in memory AND doc, and not sit under the block). Undefined → old path: table/database involved (block, memory parent or doc parent is/under one), doc parent missing in memory, or block not in the doc order.
- Used by: remote/undo add + batch add (flat index = `flatIndexForPlacement`, then `handlers.placeBlock`), `handleYjsUpdate` reparent branch and the parent-deleted-to-root branch (only when the block really leaves a parent), and `BlockManager.reparentFromHistoryReplay` via `yjsSync.placeFromDocument` (falls back to `setBlockParent`).
- New `SyncHandlers.placeBlock` (blockManager wiring: `hierarchy.mayMountHolder` → placeBlock with `dom` accordingly + syncVisibilityWithParent + announceChildPlaced). `mayMountHolder` is setBlockParent's own mount veto, so a child already in slot 2 of a two-slot parent stays there (pinned in yjs-sync-placement-replay).
- Move resync: reconcile moved-into parents' contentIds from the doc FIRST, then flat = `dfsOrder` over memory (roots sorted by doc order, unlisted-in-doc last), applied with the same `Blocks.move` loop (keeps MOVED/RENDERED hooks); holders re-seated for moved-into parents AND every parent whose child order changed. Untouched parents follow MEMORY contentIds now (base followed doc flat order).
- `memoryIndexFromDocNeighbour`/`afterRunOf` stay for the table/database fallback only. `isRunningOperationBody` kept (notion2/ins uses it).
- Harness helper for hand-built BlockYjsSync: test/unit/helpers/sync-place-block.ts.

## Step 6 part 1 notes (notion2/s6)
- Done: repository.ts topLevelBlocks comment (depth-first, not tail); `validateFlatOrder` + `FlatOrderViolation` deleted with tests; `Blocks.insertAfter` deleted with tests; api/block-placement.ts uses tree-order `subtreeEnd`; hierarchy.ts four identical subtree filters → private `subtreeOf` (kept a parentId walk, NOT `subtreeEnd`: placementForParent/placeBlock run on mid-operation arrays whose runs may be broken).
- block-mutation.ts "table-cell reorder makes it a DOM no-op" comment: already gone (removed by 30530bbb, step 3). Nothing to do.
- Kept, live: `Blocks.moveHolderInDOM`, `resortNestedBlocks`, the `isNested` local (all in `Blocks.move`, still called by block-mutation.ts:1371 moveFlat and yjs-sync.ts:2318); `insertAfterNestedBlock` (called by `insertMany`, blocks.ts:441 — the step-2 inventory was wrong about it). `Blocks.insert` flags all have live callers (block-insertion.ts:248/527, yjs-sync.ts:1759); no branch proven unreachable, none removed.
- Deferred subtree-end copies (not in s6 ownership):
  - block-insertion.ts:750 `subtreeEnd(parent)`; block-insertion.ts:647 `siblingIndexAfter` calls blocks-tree `subtreeEndIndex`.
  - yjs-sync.ts `afterRunOf`.
  - utils/blocks-tree.ts `subtreeEndIndex` (LAST index): kept. It is exported through `src/adapters.ts` (`./adapters` subpath), but the export was added after v1.15.2 (236a880e; at the tag it was a private const), so it is NOT a shipped surface. Its reader (`BlocksReader`: index getter, no contentIds, may return undefined) does not fit `BlockTreeView`. Later pass: move block-insertion.ts:647 to tree-order `subtreeEnd`, then un-export it.
  - block-mutation.ts:1789 private `subtreeEndIndex`; columns-shared.ts:57 `subtreeEndIndex(api, id)` (API-level, recursive over getChildren).
- `isUnder` copies: tree-order.ts (private), api/block-placement.ts (exported; used by api/blocks.ts:572 and its own assertCanMoveUnder), hierarchy.ts (private). Could share tree-order's once it is exported.
- blockManager.ts ~81-82 "nested children at the TAIL of the flat array" comment: owned by 5b, still to fix.
- Still open from earlier notes: hierarchy.ts's copy of the collapsed-parent hide rule (2c note), `BlockYjsSync.isRunningOperationBody` (s3 says delete, s5y says kept for notion2/ins), document-store `orderSlotForFlatIndex`/index `addBlock`/`moveBlock` (5b).

## Step 5b notes (notion2/s5b)
- Local doc writers now pass placements (addBlockAt / moveBlockTo):
  - `insertMany` 'add' path (`addBlocksToDocument`): root batch blocks follow the previous batch root (first: `lastChildBefore(null, index)`); a child of a batch block follows the nearest earlier sibling in its parent's contentIds already written; a block whose parent is OUTSIDE the batch (new, step 0 fix) → `placementsUnderExistingParents`.
  - index `insert()` + `paste()` → `BlockInsertion.addToDocumentWhereTreeHasIt`: parent + nearest earlier sibling (contentIds, or root blocks in flat order) that the DOC has. Filtering to doc-present siblings keeps order when an earlier sibling's doc add comes later.
  - `deleteSelectedBlocksAndInsertReplacement`: ONE placement for memory (`insert({placement})`) and doc: root, after `lastChildBefore(null, firstSelectedIndex)`. Was a real divergence (memory put a root block inside the toggle's run; peer save threw).
  - `clear(true)`, `restoreDefaultBlockIfDocEmptied`, `seedEmptyDocument` → `{null, null}` (empty doc; same slot as before).
  - `moveFlat` (drag move group) → `moveBlockTo(placementImpliedByFlat(tree, block, block.parentId))`. moveBlockTo skips a no-op per call, but a no-op DRAG still records an undo step on base and on this branch (coordinator review); queued in followups.
- Still index writes (table/database, per the 5c-tables DECISION): `addToDocumentWhereTreeHasIt` when the parent is/under a table/db; `insertInsideParent` selfPlaced; split tail under a table (`addSplitTailToYjs`); replacement when the first selected block's parent is/under a table/db. So `YjsManager.addBlock(…, index)`, `DocumentStore.addBlock(…, index)` and `orderSlotForFlatIndex` stay live for step 6.
- Now unused in src (tests only): `YjsManager.moveBlock(id, toIndex)` and `DocumentStore.moveBlock(id, toIndex)` (one of the two `orderSlotForFlatIndex` callers). Step 6 may delete them with their tests.
- insertMany under an existing parent skips (root fallback, as base): table/database parents, `ownsChildren` parents (column_list), and parents whose childTools refuse the block. `insertAt` would demote a refused tool; a batch block is already composed, so it is not demoted.
- Guard: test/unit/components/modules/blockManager/local-writers-doc-placement.integration.test.ts (doc == memory tree == save, undo, redo, a peer).

## Step 6 part 2 notes (notion2/s6b)
- Deleted `YjsManager.moveBlock(id, toIndex)` and `DocumentStore.moveBlock(id, toIndex)`. Tests that used them only to set up a scenario now call `moveBlockTo` with the placement the index move produced (hand-derived per site; every file's pass/expected-fail counts unchanged). Index-semantics tests (clamping, the 200 seeded moveBlock<->moveBlockTo equivalence cases) deleted. `orderSlotForFlatIndex` + index `addBlock` kept (table/db paths). undo-history.ts:2606/2651 comments still say "moveBlock" (not in s6b ownership).
- Subtree-end copies:
  - block-insertion.ts `subtreeEnd(parent)` → tree-order `subtreeEnd` (same walk: contiguous run, parentId by id, cycle set, dangling ends). Only difference: tree-order throws RangeError for a block not in the array; every caller passes a block from the array (current block / blocks[i] / getBlockById parent, and the store keeps its id index in step with the array).
  - yjs-sync.ts `afterRunOf` → tree-order `subtreeEnd`; the `start === -1 → docIndex` fallback stays in the caller. (Old code compared ancestors by block identity; every block there comes from getBlockById, which returns the first block for an id, so identity == id.)
  - LEFT block-mutation.ts private `subtreeEndIndex`: returns the LAST index (inclusive) and also takes flat-indent depth followers (`getBlockNestingDepth` deeper than the root), not only parentId descendants. Different semantics.
  - LEFT columns-shared.ts `subtreeEndIndex(api, id)`: API level, max flat index over a recursive `getChildren` (contentIds) walk, not a contiguous run; no cycle guard. Different semantics.
  - LEFT block-insertion.ts:647 `siblingIndexAfter` → blocks-tree `subtreeEndIndex` (out of the task's scope, see part-1 note).
- `isUnder` copies (tree-order private, api/block-placement exported, hierarchy.ts private, and yjs-sync.ts `isUnderBlock` in docPlacementInMemory) are identical walks. NOT consolidated: it needs `export` on tree-order.ts `isUnder`, and tree-order.ts / api/block-placement.ts were outside s6b ownership.
- hierarchy.ts `syncVisibilityWithParent` now calls `hideUnderCollapsedParent` (identical rule). The "leaving a collapsed parent" half (`hiddenByNewParent`) is a different rule and stays.
- Per-operation tree-order check (followups Step 4 (a)) landed WARN-ONLY: `BlockOperations.runOperation` wraps every public mutating method with a depth counter; `assertHierarchyInvariantInDev` marks the check while depth > 0 (runs it at once at depth 0); when the outermost call returns, `validateTreeOrder` runs and `logLabeled('Tree order broken at BlockOperations.<op>: …', 'warn')`. Skipped while `YjsManager.isInMoveGroup`. Async ops are checked when their synchronous part returns, so an operation nested AFTER an await (e.g. convert → await → replace/insert) runs at depth 0 and counts as its own outermost op (no such warning seen in the sweep). "Never throws" holds by inspection (validateTreeOrder/dfsOrder are pure and cycle-safe, logLabeled try/catches the console call), not by a try/catch. Note: the repo's default `npx vitest run` hides console output of passing tests; use `--reporter=default --silent=false` to see the warnings. Test: test/unit/components/modules/blockManager/tree-order-dev-warning.integration.test.ts.
- Review fixes (s6b): `dfsOrder` and tree-order `isUnder` are loops now (a 10000-deep chain threw RangeError, so save() failed); the dev check is wrapped in try/catch and reads `YjsManager?.`; `insertAtPlacement` now does `addToArray` → `placeBlock` → `activateBlock`, so the tool's first rendered() sees its parentId and its home slot (all placement inserts). Visible effect: a list item pasted under a list item gets its nested marker at once (base drew the root bullet); the host gets one extra `block-changed` for the FIRST pasted item (the list tool's rendered() reconciles depth to structure; that mutation lands outside the paste's atomic window — why only the first: unverified). Saved JSON, doc and undo unchanged.
